name: Test

on:
  push:
    branches:
      - master

jobs:
  testing:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: "1.15.7"

      - name: Unit
        run: cd ./backend/tests/unit && go test ./..
